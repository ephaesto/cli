import React, { ReactElement, HTMLProps } from 'react';

import classes from './jjj.module.scss';

export interface jjjProps {
  
}

export default (props: jjjProps): ReactElement<kk> => {
  return (
    < className={classes.Root}></>
  );
}