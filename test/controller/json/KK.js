import React, { ReactElement, HTMLProps } from 'react';

import classes from './kk.module.scss';

export interface kkProps {
  
}

export default (props: kkProps): ReactElement<kk> => {
  return (
    < className={classes.Root}></>
  );
}