import React, { ReactElement, HTMLProps } from 'react';

import classes from './gg.module.scss';

export interface ggProps {
  
}

export default (props: ggProps): ReactElement<ggg> => {
  return (
    < className={classes.Root}></>
  );
}