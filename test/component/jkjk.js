import React, { ReactElement, HTMLProps } from 'react';

import classes from './jkk.module.scss';

export interface jkkProps {
  
}

export default (props: jkkProps): ReactElement<> => {
  return (
    < className={classes.Root}></>
  );
}