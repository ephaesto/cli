import React, { ReactElement, HTMLProps } from 'react';

import classes from './fjjf.module.scss';

export interface fjjfProps {
  
}

export default (props: fjjfProps): ReactElement<> => {
  return (
    < className={classes.Root}></>
  );
}